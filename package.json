{
  "name": "device-management-system",
  "version": "1.0.0",
  "description": "设备管理系统 - 支持多类型设备管理、版本追踪、售后问题记录",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon server/index.js",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd client && npm install",
    "start": "node server/index.js",
    "init-db": "node -e \"require('./server/database').initializeDatabase().then(() => process.exit(0)).catch(() => process.exit(1))\"",
    "seed": "node server/seed_new.js",
    "view-data": "node view-data.js",
    "setup": "npm run install-all && npm run init-db && npm run seed",
    "test-oss": "node server/test-oss-connection.js",
    "migrate-to-oss": "node server/migrate-files-to-oss.js"
  },
  "keywords": [
    "device-management",
    "version-control",
    "issue-tracking",
    "react",
    "nodejs",
    "mysql"
  ],
  "author": "Device Management System",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "dotenv": "^16.3.1",
    "mysql2": "^3.6.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "express-rate-limit": "^7.1.5",
    "express-validator": "^7.0.1",
    "multer": "^1.4.5-lts.1",
    "ali-oss": "^6.18.0",
    "xlsx": "^0.18.5",
    "moment": "^2.29.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "concurrently": "^8.2.2"
  }
}